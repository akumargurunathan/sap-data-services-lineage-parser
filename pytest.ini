[pytest]
# Test discovery / configuration for the DS XML lineage engine.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q -m "not slow"
markers =
    slow: marks tests that parse the large (~90 MB) sample XML files
    streaming: marks tests that exercise the streaming/iterparse code path